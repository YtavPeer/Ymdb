import { SearchMoviePipesPipe } from './search-movie-pipes.pipe';

describe('SearchMoviePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMoviePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
